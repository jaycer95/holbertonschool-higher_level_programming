#!/usr/bin/python3
def multiple_returns(sentence):
    if sentense == "":
        tup = (0, None)
        return tup
    else:
        x = len(sentence)
        y = sentence[0]
        tup = (x, y)
        return tup
